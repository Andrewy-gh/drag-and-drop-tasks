// Draggable
const draggableSnapshot = {
  isDragging: true,
  draggingOver: 'column-1', // null if not over a droppable
};

// Droppable
const droppableSnapshot = {
  isDraggingOver: true, // true when a droppable is being dragged over
  draggingOverWith: 'task-1', // null when droppable is not being dragged over
};
